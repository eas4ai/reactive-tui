# Chart goldens

One `<type>_<class>.ansi` per chart type and size class (mini 20x5, medium 80x24, large 600x160), written by `tests/charts_goldens.rs` with `REGENERATE=1` and compared byte-exact otherwise.
