# Adversary brief: charts-plot-layer

## Roadmap section
## charts-plot-layer

Requirements: BAR-001, BAR-002, BAR-003, BAR-004, BAR-005, BAR-006, BAR-007, CHT-010, CHT-025, CHT-011, CHT-012, CHT-013, CHT-014, CHT-017, CHT-018, CHT-019, CHT-020, CHT-021, CHT-022, CHT-023, CHT-024, CHT-026, CHT-027, CHT-028

Deliver the shared plot layer and the cartesian charts on it: line, area,
scatter, bar (vertical and horizontal) and candlestick, with braille and eighth-block rasterization on a
worker, theme colors, the swatch-row tooltip with crosshair, mouse and
keyboard selection, value transitions, fill-parent sizing with mini, medium
and large size classes, goldens at three sizes, a catalog page and a manual section per type.

Done when every named requirement passes, each mechanism has recorded a
failing violating example before its passing receipt, the five workspace
gates pass, and the review finds no chart type mapping data outside the
plot layer.

## Frozen requirements and falsifiers
[BAR-001] Every commitment MUST leave `cargo build --locked --workspace`, `cargo fmt --all -- --check`, `cargo clippy --locked --workspace --all-targets -- -D warnings`, `cargo doc --locked --workspace --no-deps` and `cargo test --locked --workspace --no-fail-fast` passing on the Linux development host with default features.
Falsifier: Any of the five commands exits non-zero at the commitment's final commit.

[BAR-002] Every test added by a commitment MUST assert an observable outcome with an assert macro, a panic, or a `#[should_panic]` attribute; a test that only prints, only constructs values, or passes without hardware by skipping MUST be named with a `smoke_` prefix, and a hardware-gated skip MUST print `SKIP` with the reason. Tests present before this bar that violate it are repaired or renamed by the first commitment under the bar.
Falsifier: A test not named `smoke_` has no assert macro, panic or `#[should_panic]`, or a hardware-gated test returns early without printing `SKIP`.

[BAR-003] A widget delivered or reworked by a commitment MUST take every color from the active Theme through the layout's color resolver, MUST fill the rectangle its parent allots when no explicit size is given, MUST re-lay out after a terminal resize, and MUST expose every pointer action through the keyboard as well and describe its state to the screen reader.
Falsifier: A delivered widget contains a color literal, paints fewer columns than a parent wider than its default, paints stale geometry after a resize event, exposes an action only through the mouse, or has an accessibility node with no description of its state.

[BAR-004] A widget delivered or reworked by a commitment MUST render on the debug backend to checked-in goldens of the text grid plus a color digest at two sizes, one at the widget's largest useful width and at least 400 columns for a widget whose layout scales with width.
Falsifier: A delivered widget has no golden, its wide golden is narrower than 400 columns although its layout scales with width, or the golden test regenerates instead of comparing unless REGENERATE=1 is set.

[BAR-005] A widget that animates MUST keep the App's work per frame, measured from the input wait returning to the frame being presented, under 16.6 ms while animating at 700 columns by 200 rows on the debug backend; a widget whose paint cost scales with its area MUST rasterize off the main thread.
Falsifier: The measured per-frame work exceeds 16.6 ms at that size during the widget's animation, or an area-scaling widget rasterizes on the main thread.

[BAR-006] A widget delivered or reworked by a commitment MUST have a page in the widget catalog example and a section with a heading of its own in the manual, and every builder method the manual cites for it MUST exist in the code.
Falsifier: A delivered widget is absent from examples/widget_catalog, has no manual heading, or the manual cites a builder method that does not exist.

[BAR-007] A commitment MUST NOT leave a document link, script input or test path, in any form including relative `../` links, that does not exist in the tree at its final commit.
Falsifier: A tracked file references a repository path that `git ls-files` does not list.

[CHT-010] The framework MUST provide a plot layer under `src/widgets/display/charts/plot`, re-exported from `reactive_tui::widgets::display::plot`, with linear, band, point and ordinal scales, tick generation with a label-skip count, axis and grid placement, label measurement and truncation, a legend and a tooltip, and every chart renderer MUST obtain cell positions only by calling these scales.
Falsifier: Two chart types place the same value on the same axis at different cells, or a renderer file outside the plot layer divides by an axis span or otherwise maps a data value to a cell position itself.

[CHT-011] The band scale MUST support inner and outer padding, the linear scale MUST include zero when the data does not cross it, and negative values MUST draw from a zero baseline.
Falsifier: Bars touch with padding set, a positive-only series starts its axis above zero, or a negative bar grows upward from the axis bottom.

[CHT-012] Line, area and scatter charts MUST draw strokes, polygons and markers into the shared mask canvas with Natural, Linear and StepAfter curve styles and optional point dots; area charts MUST support overlaid and stacked series and a vertical fill gradient rendered as a shade ramp.
Falsifier: A diagonal line renders as detached dashes, a curve style has no visible effect at the large size class, a stacked area's upper series does not start at the lower series' top, or a scatter marker lands off its data cell.

[CHT-013] Bar charts MUST support vertical and horizontal orientation, grouped and stacked series, growth from Bottom, Top, Left or Right, per-bar color, value labels drawn in the text layer at the large size class, and MUST draw bars as rectangles with exact edge fractions into the shared mask canvas so a bar tip resolves to an eighth block.
Falsifier: A bar of value 3.5 out of 8 renders the same as 3 or 4, a Top-aligned bar grows upward, a stacked bar's segments overlap, or a large-class bar has no value label.

[CHT-014] A candlestick chart MUST draw wick and body from open, high, low and close accessors, colored by the theme's bullish and bearish colors.
Falsifier: A candle whose close is below its open uses the bullish color, or its wick does not span low to high.

[CHT-017] Every chart color, whether a series stroke, fill, bar, slice, axis, grid or tooltip swatch, MUST accept the tokens the layout utility classes accept (palette names such as `blue-500`, theme variables such as `primary`, and hex) and MUST resolve them through one resolver, `Theme::resolve_color`, that the layout's utility classes also use; the App holds the active Theme and components read it through the hook scope; default series colors MUST come from theme variables `--color-chart-1` to `--color-chart-5` plus `--color-chart-bullish` and `--color-chart-bearish`, defined in every preset with a color-blind-safe default set, with no color literal in the chart code.
Falsifier: A token that resolves in a `bg-` utility class is rejected or resolves differently in a chart, a preset lacks a chart variable, a chart renders a color not derivable from the active Theme, or a color literal appears under src/widgets/display/charts.

[CHT-018] The tooltip MUST be a box placed adjacent to the hovered or selected index (right of it, or left when the box would cross the chart's right edge, above when it would cross the bottom) with one swatch, name and value row per series at that index, at most eight rows before it summarizes, and a crosshair column or highlight band MUST mark the hovered index outside the box; keyboard navigation and the aria-live announcement MUST be retained, and the accessibility description MUST carry the series names and hovered values at every size class.
Falsifier: A tooltip crosses the chart edge, a series is missing from its rows below eight, the hovered index has no crosshair or band outside the box, or a mini chart's accessibility description omits the hovered value.

[CHT-019] Mouse movement MUST select the data index nearest on the x axis (nearest in both axes for scatter), and a mouse move that does not change the selected index MUST leave the frame unchanged outside a running transition or resize.
Falsifier: Two positions in one band select different indices, an empty cell beside a point selects nothing, or a same-index move changes the frame while nothing animates.

[CHT-020] The chart builder MUST mirror the reference's method names for each chart type delivered (`x`, `y`, `band`, `value`, `stroke`, `fill`, `natural`, `linear`, `step_after`, `dot`, `tick_margin`, `alignment`, `label`, `grid`, and for candlestick `open`, `high`, `low`, `close`) over `Vec<T>` with accessor closures that are evaluated once at `build()` into data points, so chart props stay comparable, alongside the existing series API; arguments take the terminal equivalents of the reference types.
Falsifier: A listed method is absent for a delivered chart type that supports the feature, or a builder stores a closure in the props instead of the evaluated points.

[CHT-021] Chart rasterization MUST run on a named worker thread (`rtui-chart-*`) that writes finished cells into a snapshot slot the chart owns and requests a redraw through the AppWaker; the main thread only copies the latest snapshot into the frame. A chart whose width or height is unset MUST fill its allotted rectangle (the props' size fields become optional, unset meaning fill) and MUST re-rasterize on resize, showing the previous snapshot clipped for at most one frame.
Falsifier: Shape rasterization runs on the main thread, a chart with unset size leaves its parent rectangle unpainted, or a resize leaves the previous snapshot on screen for two frames.

[CHT-022] A data change MUST animate from the previous values to the new ones over the configured duration (default 200 ms) at the App frame rate, never dropping below the previous rendering, ending exactly at the target; the reveal MUST be retained; `reduced-motion` MUST skip both.
Falsifier: A value transition dips below the previous rendering, overshoots, or stops short of its target, or `reduced-motion` still animates.

[CHT-023] Every chart type delivered in the commitment MUST have a golden of the text grid plus a color digest at each size class (mini 20 by 5, medium 80 by 24, large 600 by 160) on the debug backend, a page in the widget catalog showing all three, and a section under its own heading in the manual.
Falsifier: A delivered chart type lacks a golden at any of the three sizes, the catalog page omits a size class, or the manual heading is missing.

[CHT-024] Every chart MUST choose a size class from its allotted rectangle: mini when width is under 40 columns or height under 8 rows (no axes or legend, shapes only, usable down to 8 by 2), large when width is at least 200 and height at least 40 (grid, full labels, multi-row legend, value labels), medium otherwise (axes, ticks, single-row legend, tooltip); MUST switch class when a resize crosses a boundary; and the builder MUST allow forcing a class, which then applies at any rectangle.
Falsifier: A 39 by 10 chart draws an axis, a 200 by 40 chart has no grid, a resize from 80 by 24 to 200 by 40 keeps the medium layout, or a forced class is ignored.

[CHT-025] Every chart type delivered in a commitment MUST rasterize through one shared mask canvas: each shape (bar rectangle, line stroke, area polygon, pie sector, radar polygon, sankey ribbon, marker) is written as a per-dot membership mask at two by four dots per cell with a color token, axis-aligned rectangles also carry their exact edge fractions, and the canvas alone resolves each cell to a glyph and a color: a full block when every dot is covered, an eighth block when one rectangle edge crosses the cell at a multiple of one eighth, braille otherwise. Text (axis labels, legend, tooltip, value labels) is a separate text layer drawn over the canvas.
Falsifier: A chart type writes shape glyphs or colors to cells directly instead of masks, two chart types resolve the same coverage to different glyphs, a fully covered cell renders as braille, or a rectangle edge at 3/8 renders as a quarter.

[CHT-026] A chart with no series, an empty series, or a NaN or infinite value MUST render an explicit empty or error message in the text layer instead of shapes, and MUST never panic.
Falsifier: An empty or NaN input paints shapes, paints nothing, or panics.

[CHT-027] When a series has more points than the plot area has columns, the plot layer MUST decimate to at most two points per column by keeping each column's minimum and maximum, and the tooltip MUST still report the original index.
Falsifier: A 10,000-point series renders slower than a 1,000-point one by more than a factor of two, or the tooltip on a decimated chart reports a column index instead of a data index.

[CHT-028] When the terminal capability report says braille or block glyphs are unavailable, or the builder forces ASCII, the mask canvas MUST resolve cells with ASCII (`#`, `|`, `-`, `.`) instead, with the same geometry.
Falsifier: With glyph support reported absent, a chart emits a braille or block character.

## Mechanism definitions
assertion-audit: {"command":"python3 -B scripts/cairn/assertion_audit.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","crates/reactive-tui-suprtui","scripts/cairn","src","tests"],"requirements":["BAR-002"],"results":null}
catalog-manual: {"command":"python3 -B scripts/cairn/catalog_manual.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","examples/widget_catalog","manual/display-widgets.md","scripts/cairn","src/builder/widgets/chart.rs","src/layout/colors.rs","src/theme","src/widgets/display/charts","src/widgets/display/charts.rs","tests/common"],"requirements":["BAR-006"],"results":null}
charts-builders: {"command":"python3 -B scripts/cairn/charts_builders.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","scripts/cairn","src/builder/widgets/chart.rs","src/layout/colors.rs","src/theme","src/widgets/display/charts","src/widgets/display/charts.rs","src/widgets/display/charts/typed.rs","tests/common"],"requirements":["CHT-020"],"results":"per-requirement"}
charts-goldens: {"command":"python3 -B scripts/cairn/charts_goldens.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","examples/widget_catalog","manual/display-widgets.md","scripts/cairn","src/builder/widgets/chart.rs","src/layout/colors.rs","src/theme","src/widgets/display/charts","src/widgets/display/charts.rs","tests/charts_goldens.rs","tests/common","tests/snapshots/charts"],"requirements":["BAR-004","CHT-012","CHT-013","CHT-014","CHT-023","CHT-024","CHT-025","CHT-026","CHT-027","CHT-028"],"results":"per-requirement"}
charts-interaction: {"command":"python3 -B scripts/cairn/charts_contract.py interaction","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","scripts/cairn","src/builder/widgets/chart.rs","src/event","src/hooks","src/layout/colors.rs","src/theme","src/widgets/display/charts","src/widgets/display/charts.rs","tests/charts_contract.rs","tests/common"],"requirements":["CHT-018","CHT-019"],"results":"per-requirement"}
charts-motion: {"command":"python3 -B scripts/cairn/charts_contract.py motion","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","scripts/cairn","src/animation","src/builder/widgets/chart.rs","src/layout/colors.rs","src/theme","src/widgets/display/charts","src/widgets/display/charts.rs","tests/charts_contract.rs","tests/common"],"requirements":["CHT-022"],"results":"per-requirement"}
charts-palette: {"command":"python3 -B scripts/cairn/charts_palette.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","scripts/cairn","src/builder/widgets/chart.rs","src/layout/colors.rs","src/theme","src/widgets/display/charts","src/widgets/display/charts.rs","tests/charts_contract.rs","tests/common"],"requirements":["CHT-017"],"results":"per-requirement"}
dangling-paths: {"command":"python3 -B scripts/cairn/dangling_paths.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["CHANGELOG.md","CONTRIBUTING.md","Cargo.toml","README.md","benches","bindings","docs","examples","include","manual","scripts","src","tests","verification"],"requirements":["BAR-007"],"results":null}
frame-budget: {"command":"python3 -B scripts/cairn/charts_contract.py frame-budget","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","scripts/cairn","src/app.rs","src/backend","src/builder/widgets/chart.rs","src/layout/colors.rs","src/reactive","src/theme","src/widgets/display/charts","src/widgets/display/charts.rs","tests/charts_contract.rs","tests/common"],"requirements":["BAR-005","CHT-021"],"results":"per-requirement"}
plot-layer: {"command":"python3 -B scripts/cairn/plot_layer.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","scripts/cairn","src/builder/widgets/chart.rs","src/layout/colors.rs","src/theme","src/widgets/display/charts","src/widgets/display/charts.rs","tests/common"],"requirements":["CHT-010","CHT-011"],"results":"per-requirement"}
widget-bar: {"command":"python3 -B scripts/cairn/charts_contract.py widget-bar","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","scripts/cairn","src/builder/widgets/chart.rs","src/layout/colors.rs","src/theme","src/widgets/display/charts","src/widgets/display/charts.rs","tests/charts_contract.rs","tests/common"],"requirements":["BAR-003"],"results":"per-requirement"}
workspace-gates: {"command":"python3 -B scripts/cairn/workspace_gates.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","benches","build.rs","crates","examples","scripts/cairn","src","tests"],"requirements":["BAR-001"],"results":null}

## Builder claims
mechanism assertion-audit Q1 observed: Receipt f686f05df411: the tree before commit fb5c1e37, where tests had no assertion; the check printed 'BAR-002 violated: tests without an assertion and not named smoke_:' and listed them, first tests/api_widget_behavior/wizard.rs::empty_wizard_displays_error_and_can_cancel (122 lines).
mechanism assertion-audit Q2 observed: The script ran to completion and listed test functions by path and name; each named test really had no assert macro, panic or #[should_panic] (fb5c1e37 added the assertion or the smoke_ prefix to every one), which is the falsifier's first clause, not a parse or toolchain error.
mechanism catalog-manual Q1 observed: Receipt 18c1a12d3902: the tree before commit 33bc6700, with no chart catalog page and no chart headings in the manual; printed 'BAR-006 violated: manual has no heading for the line chart; catalog has no area chart page; ... manual does not describe the mini size class' (13 lines).
mechanism catalog-manual Q2 observed: Each line names a missing catalog page or manual heading for a delivered chart type, the falsifier's first two clauses; the script found examples/widget_catalog and manual/ and read them, so the failure came from their content, not from a missing input.
mechanism charts-builders Q1 observed: Receipt 31175c7e6c85 on fixture commit f5b627d8, which renamed pub fn step_after to stepped in src/widgets/display/charts/typed.rs (4 occurrences); printed 'cairn: CHT-020: fail' with reason 'area: .step_after() missing; line: .step_after() missing'.
mechanism charts-builders Q2 observed: The reason names exactly the method the fixture removed, on the two chart types that must expose it, which is the falsifier's first clause; the revert commit 518759ae restored the file byte for byte (git diff --stat HEAD~2 HEAD -- typed.rs was empty) and receipt dc48950462b4 then passed.
mechanism charts-goldens Q1 observed: Receipt 8da6bdb2107e on the tree before commit c9f807a1: 'CHT-012 reason: diagonals must be braille strokes, not stepped dashes (dashes 27, braille 0)', 'CHT-013 reason: tip must be an eighth block', 'CHT-024 reason: 39 columns is the mini class: no axis or legend', 'CHT-025 reason: a steep diagonal must produce braille cells', 'CHT-027 reason: 1000 points did not paint three frames inside 3 s', 'CHT-014 reason: no candlestick chart type in chart code', 'CHT-028 reason: no ASCII glyph fallback', 'CHT-023 reason: missing golden line_mini.ansi ...', 'BAR-004 reason: no checked-in wide golden'. Receipt bba5d5a5e60e on fixture commit 1b91e05e, which blanked the 'No data to display' string in live/canvas.rs and canvas/pie.rs: 'CHT-026 reason: empty chart must say so'.
mechanism charts-goldens Q2 observed: Every reason is the requirement's own falsifier clause measured on the old chart (dashes instead of braille, no eighth block, axis drawn at 39 columns, no goldens, no candlestick type, no ASCII set), and each cargo test line ran to a result rather than a compile or harness error; for CHT-026 the only change was the message string, and the golden test's own assertion text is what printed, while the other nine results in that receipt passed.
mechanism charts-interaction Q1 observed: Receipt 33a85a35d8b1 on the tree before c9f807a1: 'CHT-018 reason: tooltip must be a box, got:' followed by the full-width band frame, and 'CHT-019 reason: hovering the empty cell next to a point must select the nearest index, got:' with an unselected frame.
mechanism charts-interaction Q2 observed: The old widget drew a full-width tooltip band and selected only on a point's own cell, which are the falsifier clauses 'a tooltip crosses the chart edge' and 'an empty cell beside a point selects nothing'; both tests ran through the App harness and printed frames, so the failure was in the rendered output, not setup.
mechanism charts-motion Q1 observed: Receipt 15e0815db312 on the tree before c9f807a1: 'CHT-022 reason: frame 2 dropped to 0 blocks, below the previous rendering (210): the transition restarted from zero instead of moving from the old values'.
mechanism charts-motion Q2 observed: The test counted painted block cells across frames and saw 210 then 0, the falsifier's 'dips below the previous rendering'; a setup failure would have painted nothing on frame 1 as well.
mechanism charts-palette Q1 observed: Receipt 0c83655751fb on the tree before c9f807a1: 'CHT-017 reason: hex literal "#3b82f6" at src/widgets/display/charts.rs:206; "#ef4444" at :207; "#10b981" at :208; "#f59e0b" ...'.
mechanism charts-palette Q2 observed: The reason cites file and line of color literals under src/widgets/display/charts, the falsifier's last clause; the literals were the old hard-coded palette that c9f807a1 replaced with chart-1..chart-5 theme tokens resolved through Theme::resolve_color.
mechanism dangling-paths Q1 observed: Receipt 60330a847b77 on fixture commit 26df91f4, which appended 'See scripts/no-such-file.py for the fixture.' to CONTRIBUTING.md; printed 'BAR-007 violated: references to paths git does not track:' and 'CONTRIBUTING.md: scripts/no-such-file.py'.
mechanism dangling-paths Q2 observed: The one finding is the exact path the fixture added, in the file it was added to, and nothing else; the revert 6ceb773f removed the line and receipt 6a6b73610b2c passed on the same declaration, so the failure was the falsifier ('a tracked file references a repository path that git ls-files does not list') and not a scan error.
mechanism frame-budget Q1 observed: Receipt c4c9cf63a485 on the tree before c9f807a1: 'CHT-021 reason: a chart with no explicit size must fill a 140-column parent, painted 80 columns' and 'BAR-005 reason: App painted 4 frames but input needs Some((12, [])) before the deadline ... fewer than 12 frames painted inside the harness's 3 s deadline at 700x200: per-frame work exceeds 16.6 ms'.
mechanism frame-budget Q2 observed: The old chart had a fixed 80 by 20 default and painted on the main thread, so it painted 80 columns in a 140-column parent and could not reach 12 frames at 700 by 200: the falsifiers 'leaves its parent rectangle unpainted' and 'per-frame work exceeds 16.6 ms'. The harness delivered input and counted painted frames, so the App ran; only the count was short.
mechanism plot-layer Q1 observed: Receipt 799eaf9ad9ab on the tree before c9f807a1: 'CHT-010 reason: no plot layer directory (src/widgets/display/charts/plot or src/plot); local scale arithmetic at src/widgets/display/charts/live/canvas/cartesian.rs:20 ...' and 'CHT-011 reason: no ScaleBand with padding_inner/padding_outer; four padded bars expected on the top row'.
mechanism plot-layer Q2 observed: The reasons name the missing plot module and the exact renderer line that divided by an axis span, the falsifier's 'a renderer file outside the plot layer ... maps a data value to a cell position itself', and the bar test found touching bars with padding set; the mechanism compiled and ran the contract test, so this was the behavior, not the setup.
mechanism widget-bar Q1 observed: Receipt 4435e9d56042 on the tree before c9f807a1: 'BAR-003 reason: 16 hard-coded colors in chart code; 1 passed'.
mechanism widget-bar Q2 observed: The count is of color literals in the old chart source, the falsifier's 'a delivered widget contains a color literal'; the rest of the BAR-003 test passed in the same run, so the harness and resolver were working.
mechanism workspace-gates Q1 observed: Receipt a7e5e605096e: cargo clippy --locked --workspace --all-targets -- -D warnings failed in crates/reactive-tui-libghostty-vt with 'error: this method could have a #[must_use] attribute' repeated and 'could not compile reactive-tui-libghostty-vt (lib) due to 138 previous errors' (83 lines).
mechanism workspace-gates Q2 observed: Clippy is one of the five commands BAR-001 names and it exited non-zero on real lints in a workspace crate, the falsifier as written; cargo itself ran and compiled the dependencies, so the toolchain was present. Commit 80ec7422 fixed the lints and receipt 9baae45d1399 then passed all five commands.
requirement BAR-001 Q3 observed: Receipt 9baae45d1399 ran build, fmt --check, clippy -D warnings, doc --no-deps and test --workspace --no-fail-fast at commit acaafc3c and all five exited zero (117 s with warm caches).
requirement BAR-001 Q4 observed: The clippy fix 80ec7422 changed 20 files in crates/reactive-tui-libghostty-vt (must_use attributes, doc markers); the test sweep fb5c1e37 touched 51 files including src/layout, src/hooks and src/core test modules. Nothing outside the five commands checks them.
requirement BAR-002 Q3 observed: scripts/cairn/assertion_audit.py scans every #[test] in tests/ and src/ (622 files) for an assert macro, panic!, unreachable! or #[should_panic]; fb5c1e37 added an assertion or the smoke_ prefix to every test it listed, and receipt 65b27cbfa002 reports zero findings.
requirement BAR-002 Q4 observed: fb5c1e37 renamed tests and added assertions across tests/api_widget_behavior, src/layout, src/hooks and src/core; a renamed test changes nothing the suite checks, but a test that got a weak assertion (asserting a length rather than content) is still counted as asserting.
requirement BAR-003 Q3 observed: tests/charts_contract.rs bar_003_ finds no color literal under src/widgets/display/charts (colors are chart-1..chart-5 tokens through Theme::resolve_color), paints a size-unset chart to a 140-column parent at full width, re-lays out after a resize event, moves selection with Left/Right/Home/End and Escape, and reads the accessibility description of the selected value; receipt 3e2b6b6453c7 passes.
requirement BAR-003 Q4 observed: The keyboard path and the description live in live.rs; the test drives them through the App harness with the debug backend only. Pointer actions on the crossterm backend go through the same handle_event code but are not exercised by the check.
requirement BAR-004 Q3 observed: tests/snapshots holds goldens for every delivered chart type at mini, medium and large (600 columns), each a text grid plus a color digest, and tests/charts_goldens.rs compares against them unless REGENERATE=1; receipt 3e5040196c1531 reports BAR-004 pass with 'goldens at two sizes, wide one 600 columns'.
requirement BAR-004 Q4 observed: Goldens were regenerated three times during the commitment (worker timing, cell grid). Each regeneration was reviewed by eye for shape, but a regression that changed only a color digest would pass a regeneration without anyone reading it.
requirement BAR-005 Q3 observed: tests/charts_contract.rs bar_005_ animates a 700 by 200 chart on the release build and requires 12 painted frames inside 3 s with per-frame work under 16.6 ms; measured work is 2.5 ms with the grid off and 3.5 ms with grid and legend since the cell-grid element (2320cff0). Rasterization runs on the rtui-chart-N worker, recorded by the CHART_WORKER_SEEN static. Receipt d60fb3af0009 passes.
requirement BAR-005 Q4 observed: The budget is measured only on --release; in debug the test prints SKIP after checking the worker. 2320cff0 also changed the runtime for every component (update_shared pointer fast path, compare-before-clone in update_any) and added PaintSpec.cells to the bridge; those are covered by the full suite, not by a frame-budget check.
requirement BAR-006 Q3 observed: examples/widget_catalog/catalog.rs has CatalogPage::Charts (key 5) showing every chart type at three size classes, manual/display-widgets.md has a heading per chart type and per size class, and scripts/cairn/catalog_manual.py verifies every builder method the manual cites exists in the code; receipt 849721062b11 passes.
requirement BAR-006 Q4 observed: The catalog page count went from 8 to 9, which moved the Motion page to key 8; tests/widget_catalog_behavior.rs was updated for it. The manual's prose about rendering quality is not checked by anything.
requirement BAR-007 Q3 observed: scripts/cairn/dangling_paths.py scans every tracked text file, resolving references against the repository root and any enclosing package root, and receipt 6a6b73610b2c reports zero references to untracked paths after commits a9687171 and 2bc0660d.
requirement BAR-007 Q4 observed: 2bc0660d added package_roots() and an exemption for dated baselines under scripts/abi/baselines; 1ed5e06f widened the declaration to every directory the script scans. a9687171 rewrote comments and manual prose in 21 files to name paths that exist; the prose change is not checked by any test.
requirement CHT-010 Q3 observed: src/widgets/display/charts/plot holds ScaleLinear, ScaleBand, ScalePoint, ScaleOrdinal, ticks with a skip count, Axis, Grid, Legend and Tooltip, re-exported from reactive_tui::widgets::display::plot; live/canvas/cartesian.rs makes 13 scale calls and the only division in it normalizes a fill position inside a polygon for the shade ramp (cell space, not data space). scripts/cairn/plot_layer.py verifies the plot directory exports pub ScaleLinear, ScaleBand, ScalePoint, ScaleOrdinal, Tick, Axis, Grid, Legend and Tooltip, that cartesian.rs, pie.rs and canvas.rs reference plot::, and that no renderer file outside plot/ contains local scale arithmetic (a division by a range, a span or a tuple extent); receipt 3d12b9d84634 reports 'plot layer present and used by every renderer'.
requirement CHT-010 Q4 observed: live/canvas/pie.rs exists for the next commitment's radial charts and uses angles, not scales; it is compiled and its empty-data message is exercised by CHT-026, but nothing in this commitment's checks covers its geometry.
requirement CHT-011 Q3 observed: ScaleBand takes padding_inner and padding_outer; ScaleLinear::nice includes zero when the data does not cross it; bars grow from the zero baseline in cartesian.rs. cht_011_ asserts four padded bars on the top row, an axis that starts at zero for a positive series and a negative bar drawn below the axis; receipt 3d12b9d84634 passes.
requirement CHT-011 Q4 observed: The nice-tick algorithm was written for this commitment; its label formatting for very large or very small magnitudes is only what the goldens happen to show.
requirement CHT-012 Q3 observed: Strokes, polygons and markers go through MaskCanvas (mask.rs) with Curve::Natural, Linear and StepAfter, dots, stacked and overlaid areas and a shade-ramp gradient; goldens cht_012_ assert braille diagonals, a visible curve difference at the large class, stacked upper series starting at the lower top and markers on their data cell; receipt 3e5040196c1531 passes.
requirement CHT-012 Q4 observed: The curve module (plot/curve.rs) and decimate_min_max are shared with the tooltip's index mapping; the natural spline's behavior on two-point series is covered only by the mini golden.
requirement CHT-013 Q3 observed: Bars are written as rectangles with eighth edge fractions into the mask canvas, with BarGrowth Bottom/Top/Left/Right, grouped and stacked series, per-bar color and value labels in the text layer at the large class; cht_013_ asserts a 3.5-of-8 bar differs from 3 and 4 and that the tip is an eighth block; receipt 3e5040196c1531 passes.
requirement CHT-013 Q4 observed: Horizontal bars use the same rectangle path rotated; the horizontal value-label placement is covered by the bar goldens only.
requirement CHT-014 Q3 observed: ChartType::Candlestick with DataPoint.candle() draws wick from low to high and body from open to close, colored by the theme's bullish and bearish tokens; cht_014_ asserts a bearish candle takes the bearish color and the wick spans low to high; receipt 3e5040196c1531 passes.
requirement CHT-014 Q4 observed: The typed CandlestickBuilder (open/high/low/close accessors) evaluates closures at build; candles with equal open and close render a one-row body, which only the golden shows.
requirement CHT-017 Q3 observed: Every chart color goes through Theme::resolve_color, the same resolver the bg- utility classes use, and tests/charts_contract.rs cht_017_ resolves the same token through both paths and scans src/widgets/display/charts for hex literals; every preset in src/theme defines chart-1..chart-5; receipt ee8c988ef33f passes.
requirement CHT-017 Q4 observed: Two theme presets gained chart variables in c9f807a1; a custom theme loaded from a file without chart-N variables falls back to the resolver's default, which the check does not exercise.
requirement CHT-018 Q3 observed: The tooltip is a box drawn by overlay_grid in live.rs, placed right of the index or flipped left or up at the edges, one swatch/name/value row per series, summarized past eight rows, with a crosshair column; the mini class writes the hovered value into the accessibility description. cht_018_ asserts the box never crosses an edge and every series appears; receipt e073f654aa79 passes.
requirement CHT-018 Q4 observed: overlay_grid clones the worker's CellGrid on every hover change and draws the box over it on the main thread; its cost is inside the BAR-005 budget but not measured separately.
requirement CHT-019 Q3 observed: Mouse moves select the nearest index on x (nearest in both axes for scatter) through the picture's index_columns table, and a same-index move returns without re-rendering. cht_019_ asserts the empty cell beside a point selects its neighbour and that a same-index move leaves the frame unchanged; receipt e073f654aa79 passes.
requirement CHT-019 Q4 observed: Mouse events are delivered through the App harness on the debug backend; crossterm mouse capture flags are unchanged and not covered here.
requirement CHT-020 Q3 observed: src/widgets/display/charts/typed.rs exposes x, y, band, value, stroke, fill, natural, linear, step_after, dot, tick_margin, alignment, label, grid and open/high/low/close on the delivered builders over Vec<T> with accessor closures evaluated at build() into DataPoints; scripts/cairn/charts_builders.py verifies each name per chart type and that no Box/Arc/Rc<dyn Fn> field exists in chart props; receipt dc48950462b4 passes.
requirement CHT-020 Q4 observed: The closure-field scan covers charts.rs only, not typed.rs; typed.rs builders hold closures until build() by design and ChartProps derives PartialEq, so a stored closure would not compile, but the mechanism does not itself prove that.
requirement CHT-021 Q3 observed: live/worker.rs runs rasterization on a thread named rtui-chart-N that writes a Picture into the chart's slot and wakes the App; the main thread attaches the Arc<CellGrid> to one element. Width or height of 0 fills the parent. cht_021_ asserts a size-unset chart fills a 140-column parent and that a resize replaces the picture within one frame; receipt d60fb3af0009 passes.
requirement CHT-021 Q4 observed: First-picture and resize waits (200 ms and 60 ms) were added to avoid blank frames; the waits are part of the frame-budget measurement but no check bounds them directly.
requirement CHT-022 Q3 observed: live/motion.rs interpolates from the previous values to the new ones over transition_duration (default 200 ms), clamps at the target, keeps the reveal one-shot per data appearance and skips both under reduced-motion; cht_022_ counts painted blocks per frame and asserts they never drop below the previous frame and end at the target glyph; receipt f0dbf22968fa passes.
requirement CHT-022 Q4 observed: The reveal state became (Option<Instant>, bool) to stop it replaying when animated is enabled later; that fix is covered by the motion test's second phase only.
requirement CHT-023 Q3 observed: Every delivered chart type has goldens at mini 20 by 5, medium 80 by 24 and large 600 by 160 in tests/snapshots, a catalog page showing all three classes, and a manual heading; scripts/cairn/charts_goldens.py checks the golden files, the REGENERATE guard and the manual; receipt 3e5040196c1531 passes.
requirement CHT-023 Q4 observed: The large goldens are 600 by 160 rather than the 200 by 40 threshold, so the large class is exercised only at that one size.
requirement CHT-024 Q3 observed: SizeClass::from(rect) chooses mini under 40 columns or 8 rows, large at 200 by 40 or more, medium otherwise, with a forced class honoured; cht_024_ asserts no axis at 39 by 10, a grid at 200 by 40, a re-layout on resize from 80 by 24 to 200 by 40 and a forced class; receipt 3e5040196c1531 passes.
requirement CHT-024 Q4 observed: The legend's multi-row layout at the large class depends on series count; goldens cover three series.
requirement CHT-025 Q3 observed: mask.rs holds one MaskCanvas at two by four dots per cell with edge fractions for axis-aligned rectangles; every shape is written as a mask with a color token and compose() resolves cells to braille, eighth blocks or full blocks by coverage; cht_025_ asserts a steep diagonal produces braille, a full cell is a block and a 3/8 edge is an eighth; receipt 3e5040196c1531 passes.
requirement CHT-025 Q4 observed: Color resolution when two shapes of different tokens cover one cell takes the later shape; the goldens show the outcome but no test names that rule.
requirement CHT-026 Q3 observed: live/canvas.rs and canvas/pie.rs write 'No data to display' into the text layer for no series, an empty series or a NaN or infinite value and draw no shapes; cht_026_ feeds each case and asserts the message, no shape glyph and no panic; receipt 3e5040196c1531 passes, and receipt bba5d5a5e60e failed when the message was blanked (fixture 1b91e05e).
requirement CHT-026 Q4 observed: The message is not localized and the check matches its English text; a NaN inside an otherwise valid series blanks the whole chart rather than the one point.
requirement CHT-027 Q3 observed: plot/decimate.rs keeps each column's minimum and maximum when a series has more points than columns, and the tooltip maps through the kept index table to the original index; cht_027_ hovers column 100 of a 10,000-point chart and asserts the reported index is at least 1000, and on the release build asserts the 10,000-point render costs at most twice the 1,000-point one (measured 8 ms versus 2.5 ms); receipt 3e5040196c1531 passes.
requirement CHT-027 Q4 observed: The timing half runs only on --release; on debug the test asserts the index mapping and prints SKIP for the ratio.
requirement CHT-028 Q3 observed: GlyphSet::Ascii in mask.rs resolves cells with #, |, - and . using the same coverage geometry, chosen when the capability report lacks braille or block glyphs or the builder sets ascii; cht_028_ forces ASCII and asserts no braille or block character appears while the golden geometry matches; receipt 3e5040196c1531 passes.
requirement CHT-028 Q4 observed: The capability report path is driven by the builder flag in tests; a terminal that reports no braille support at runtime takes the same branch but is not exercised.
commitment charts-plot-layer Q5 observed: Every timing check (BAR-005, CHT-027) is a release-build measurement on this host with a 3 s harness deadline; a slower host or a debug-only CI would print SKIP and pass. Goldens fix the debug backend's text grid and color digest, not what a real terminal draws for braille and eighth blocks. The roadmap's review question, no chart type mapping data outside the plot layer, was checked by scanning src/widgets/display/charts for scale calls versus span divisions: cartesian.rs has 13 scale calls and one division, which normalizes a fill position for the shade ramp in cell space; pie.rs uses angles and belongs to the next commitment. The runtime fast paths in 2320cff0 (Arc pointer equality before comparing props) apply to every component; a component that mutates props in place behind an Arc would be skipped, and only the full suite guards that.
commitment charts-plot-layer Q6 observed: Not tested: rendering on a real kitty, ghostty or Windows Terminal (goldens are the debug backend); macOS and Windows hosts; the crossterm backend's mouse path for charts; series above 10,000 points; resize storms during a transition; themes loaded from files without chart-N variables; the pie canvas geometry (next commitment); long-running animation drift beyond the 12-frame budget window.

## Builder findings

## Interface obligations

## Exclusion manifest (classes and paths, never contents)

## Boundary
Harness claude_code, model any, transport any.
You may read only the projection directory. Boundary: unenforced.
This projection omits network_exclude and credential paths. It is an egress boundary, not an information-flow proof: it cannot detect a secret a person or primary coding agent copied into ordinary prose, and it does not govern Git pushes.

## Your work
For each mechanism (Q1, Q2): try to make it pass without the behavior, make it fail for a setup reason, and find an input it reads but does not declare. For each Q3: try to reach the falsifier with an input. For each Q4: find touched paths the claim omitted. For Q5 and Q6: look where the builder said not to. Every interface obligation gets a caller-level attempt. Write one attempt per question and target, one per interface path, and number your findings from 1.
