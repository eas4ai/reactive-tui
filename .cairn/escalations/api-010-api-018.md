DECISION

Question:   May I add a public cycle_duration: Option<Duration> field to GradientBorder so rainbow_border can deliver its advertised automatic color cycling through App?
Recommend:  Add the field. GradientBorder::new keeps a static border with cycle_duration set to None; rainbow_border sets a two-second cycle. App owns the clock, preserves it across keyed redraws, and removes it on unmount. Existing struct-literal callers add cycle_duration: None; constructor calls keep their signatures. Test intermediate border frames, explicit static borders, keyed continuity, removal and App isolation.
Because:    GradientBorder currently has only public gradient and width fields. rainbow_border returns exactly the same value as a manually constructed static red-green-blue border, despite promising an animated border. The painter cannot distinguish the two without explicit configuration. Inferring animation from matching colors would also animate intentionally static borders. The commitment requires repairing advertised behavior while preserving working APIs, so this source-compatibility change needs your approval.
If wrong:   Rust callers that construct GradientBorder with a struct literal must add the new field before recompiling. Existing constructors and the C ABI keep their signatures. The current API-010 work remains uncommitted; its 24 captured-frame tests and six motion tests passed locally, but formal acceptance and the remaining remediation are unfinished.
Instead:    Keep the two-field type and explicitly approve correcting rainbow_border documentation to promise a static rainbow border. Add a separate opt-in animation API for callers that want cycling. This preserves struct-literal source compatibility but narrows the existing advertised behavior, which the commitment currently forbids without your decision.

Reply: ok | instead | ask. If this isn't clear, ask me to explain it another way before you decide.

Concerns: API-010,API-018
Status: open
Raised: 2026-09-09T13:24:32.029Z
Raised after: API-010=1 API-018=0
Answer: ok
Answered: 2026-09-09T13:25:52.523Z
Answered after: API-010=1 API-018=0
Answered order: 4
