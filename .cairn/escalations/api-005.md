DECISION

Question:   May I add an owned metadata field to Element and require builder event callbacks to be Send + Sync, accepting these two Rust source-compatibility changes?
Recommend:  Approve owned Element metadata and thread-safe callbacks. Existing Element struct literals add metadata: Default::default(); constructors and builders keep their normal syntax. Callbacks that capture Rc or RefCell migrate to Arc or Mutex. Existing C ABI signatures remain unchanged.
Because:    The current Element struct exposes every field, and on_click accepts Fn() without Send or Sync while discarding it. Element props and the existing EventRouter require Send + Sync. Directly owned handlers give callbacks the same lifetime as their element and let App route them safely; these source changes conflict with the commitment instruction to preserve working Rust APIs, so I need your decision before implementing them.
If wrong:   Downstream Rust code using Element struct literals or non-thread-safe callback captures will stop compiling until migrated. We do not know all downstream callers. Repository call sites and compatibility examples will be updated and tested in this commitment.
Instead:    Keep the existing Rust source shapes and callback bounds. I would design a thread-bound callback registry and separate metadata attachment, with explicit cross-thread invocation and cleanup rules; this adds lifetime machinery and restrictions instead of direct ownership. Reply with that preference or another compatibility constraint.

Reply: ok | instead | ask. If this isn't clear, ask me to explain it another way before you decide.

Concerns: API-005
Status: open
Raised: 2026-09-09T00:26:39.613Z
Raised after: API-005=0
Answer: ok
Answered: 2026-09-09T01:27:20.280Z
Answered after: API-005=0
Answered order: 2
