DECISION

Question:   May UnixTty::spawn_input_thread and DirectTty::start_async_events return InputReceiver<T> instead of std::sync::mpsc::Receiver<T>?
Recommend:  Yes. Make dropping the receiver or final terminal owner stop and join its input workers, even while idle. Preserve normal receive methods and bound queues.
Because:    Probes found idle worker leaks and a worker reading a socket that reused the terminal descriptor. The proposed receiver owns cancellation. Evidence and migration examples: .cairn/reviews/api-019-input-lifecycle/review.md.
If wrong:   Callers explicitly requiring the standard receiver or its iterator types will need migration. Inferred calls such as input.recv() keep their usual form.
Instead:    Keep the existing return types and approve an idle legacy worker remaining until terminal teardown. Add a separate owned-receiver route for prompt consumer cleanup.

Reply: ok | instead | ask. If this isn't clear, ask me to explain it another way before you decide. If this isn't clear, ask me to explain it another way before you decide.

Concerns: API-019 API-020
Status: open
Raised: 2026-09-12T21:41:30.179Z
Raised after: API-019=1 API-020=0
Answer: ok
Answered: 2026-09-13T00:04:56.447Z
Answered after: API-019=1 API-020=0
Answered order: 11
