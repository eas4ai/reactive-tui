# Adversary brief: suprtui

## Roadmap section
## suprtui

Requirements: BAR-001, BAR-002, BAR-003, BAR-004, BAR-005, BAR-006, BAR-007, RAS-001, RAS-002, RAS-003, RAS-004, RAS-005, RAS-006, RAS-007, RAS-008, PNT-001, PNT-002, PNT-003, PNT-004, PIP-001, PIP-002, BLT-001, BLT-002

Supersedes suprtui-renderer by the developer's ruling on 2026-09-23. The
developer raised RAS-005's unchanged-frame bound to 1 millisecond while
suprtui-renderer was open, and a started commitment's frozen contract is
not amended, so this commitment freezes the revised text. Its
requirements and done-when are those of suprtui-renderer, and the
rasterizer, presentation, painter and blitter work delivered under it
carries over unchanged.

Deliver the SuprTUI renderer plan already landed under suprtui-renderer:
check every requirement on the current code, record each mechanism's
review against the frozen text, and close with the commitment review.

Done when every named requirement passes, the manual sentence in PIP-002
is updated, and the review finds no per-cell allocation or per-cell reset
on the render path.

## Frozen requirements and falsifiers
[BAR-001] Every commitment MUST leave `cargo build --locked --workspace`, `cargo fmt --all -- --check`, `cargo clippy --locked --workspace --all-targets -- -D warnings`, `cargo doc --locked --workspace --no-deps` and `cargo test --locked --workspace --no-fail-fast` passing on the Linux development host with default features.
Falsifier: Any of the five commands exits non-zero at the commitment's final commit.

[BAR-002] Every test added by a commitment MUST assert an observable outcome with an assert macro, a panic, or a `#[should_panic]` attribute; a test that only prints, only constructs values, or passes without hardware by skipping MUST be named with a `smoke_` prefix, and a hardware-gated skip MUST print `SKIP` with the reason. Tests present before this bar that violate it are repaired or renamed by the first commitment under the bar.
Falsifier: A test not named `smoke_` has no assert macro, panic or `#[should_panic]`, or a hardware-gated test returns early without printing `SKIP`.

[BAR-003] A widget delivered or reworked by a commitment MUST take every color from the active Theme through the layout's color resolver, MUST fill the rectangle its parent allots when no explicit size is given, MUST re-lay out after a terminal resize, and MUST expose every pointer action through the keyboard as well and describe its state to the screen reader.
Falsifier: A delivered widget contains a color literal, paints fewer columns than a parent wider than its default, paints stale geometry after a resize event, exposes an action only through the mouse, or has an accessibility node with no description of its state.

[BAR-004] A widget delivered or reworked by a commitment MUST render on the debug backend to checked-in goldens of the text grid plus a color digest at two sizes, one at the widget's largest useful width and at least 400 columns for a widget whose layout scales with width.
Falsifier: A delivered widget has no golden, its wide golden is narrower than 400 columns although its layout scales with width, or the golden test regenerates instead of comparing unless REGENERATE=1 is set.

[BAR-005] A widget that animates MUST keep the App's work per frame, measured from the input wait returning to the frame being presented, under 16.6 ms while animating at 700 columns by 200 rows on the debug backend; a widget whose paint cost scales with its area MUST rasterize off the main thread.
Falsifier: The measured per-frame work exceeds 16.6 ms at that size during the widget's animation, or an area-scaling widget rasterizes on the main thread.

[BAR-006] A widget delivered or reworked by a commitment MUST have a page in the widget catalog example and a section with a heading of its own in the manual, and every builder method the manual cites for it MUST exist in the code.
Falsifier: A delivered widget is absent from examples/widget_catalog, has no manual heading, or the manual cites a builder method that does not exist.

[BAR-007] A commitment MUST NOT leave a document link, script input or test path, in any form including relative `../` links, that does not exist in the tree at its final commit.
Falsifier: A tracked file references a repository path that `git ls-files` does not list.

[BLT-001] Image fallback MUST offer half-block, quadrant, sextant (U+1FB00 to U+1FB3B), octant (U+1CD00 to U+1CDE5) and braille (U+2800 to U+28FF) blitters; for each pixel block the glyph and its two colors MUST be the two-partition of the block's pixels with the minimum total color error, with any transparent pixel forcing a transparent background.
Falsifier: An exhaustive check over a fixed block finds a partition with lower total error than the one chosen, or a block with a transparent pixel renders an opaque background.

[BLT-002] The blitter MUST be chosen by tier, braille, octant, sextant, quadrant, half-block, ASCII: ASCII when the capability report says no unicode, otherwise the tier a per-terminal table gives the host's identity, sextant when the host is not in the table; an application or environment override MUST replace the choice.
Falsifier: A host reporting no unicode receives a block glyph above ASCII, a host in the table receives a tier other than its entry without an override, or an override is ignored.

[PIP-001] `present` MUST return the frame's geometry as soon as layout and paint finish; the worker MUST write and flush the bytes afterwards; a present MUST wait for the previous frame's flush before submitting, so at most one frame is in flight.
Falsifier: `present`'s wall time includes the flush of the frame it submitted (measured with a slow writer), or two frames are in flight.

[PIP-002] A flush failure MUST be reported by the next `present` or by `shutdown`, MUST force a full repaint of the next frame, and the App MUST keep the geometry of the last frame whose flush was acknowledged.
Falsifier: A failed flush is never reported, or the frame after a failure is emitted as a diff.

[PNT-001] A node with an identity transform and no mask MUST fill its background by row and paint its text without a per-cell inverse transform; masked or transformed nodes keep the general path and both paths MUST produce the same cells.
Falsifier: A per-cell inverse transform runs for an identity node, or the fast path's cells differ from the general path's on any golden.

[PNT-002] The Backend MUST offer a per-cell hit query answered from the renderer's committed hit grid, which the painter fills with element indices as it paints, exact under masks, transforms and z-order, committed and rolled back with the frame; the event layer MUST prefer the query when offered and fall back to painted bounds otherwise; the per-cell mask scan in `hit_bounds` MUST be removed.
Falsifier: A click inside an element's bounding rectangle but outside its mask dispatches to that element, or a click on a cell painted by a higher z-order sibling dispatches to the lower one.

[PNT-003] One `Element` copy per present: `render_frame` MUST store a shared handle and `present` MUST send that handle.
Falsifier: Two deep copies of the frame element are observed per present.

[PNT-004] When the paint spec is unchanged since the last frame at the same size, layout MUST not be recomputed.
Falsifier: Presenting an unchanged spec re-runs layout.

[RAS-001] The rasterizer MUST track the terminal cursor across the glyphs it emits within a frame, advancing by each glyph's cell width; a changed cell at the tracked position MUST emit no cursor move; a changed cell elsewhere on the tracked row MUST move with CHA (`ESC [ n G`); every other move MUST use CUP.
Falsifier: Two horizontally adjacent changed cells produce two cursor-move sequences, or a same-row jump emits CUP.

[RAS-002] Foreground, background, base attributes and decoration MUST be emitted only when they differ from the last cell emitted in the frame; an attribute that turns off MUST use its SGR off code (22, 23, 24, 25, 27, 28, 29, 55); `ESC [0m` MUST appear at most once after the frame's sync-set and once before its sync-reset.
Falsifier: Consecutive changed cells of one style contain more than one foreground sequence, or a per-cell `ESC [0m` appears.

[RAS-003] Cell emission MUST append to the frame's byte buffer directly with integer formatting; no `String` or `Vec` MUST be created per cell on the render path.
Falsifier: A counting allocator reports any allocation during `render` of a 10,000-cell frame once the frame buffer has reached capacity.

[RAS-004] For every renderer test frame and every charts golden, the byte stream replayed through the vt100 and ghostty parser models MUST produce the screen text and colors recorded in tests/snapshots/renderer before the rasterizer changed.
Falsifier: A replay differs from its recording in a cell's text or color, or a frame has no recording.

[RAS-005] In a release build on the development host, best of three runs, a full 200 by 50 text repaint MUST emit at most 12 bytes per cell, an unchanged 262,144-cell frame MUST cost at most 1 millisecond and a full repaint of it at most 15 milliseconds.
Falsifier: The bench exceeds any bound on all three runs.

[RAS-006] `RenderStats` MUST report bytes emitted, cursor moves emitted and elided, foreground, background and attribute emissions and elisions, and layout, diff, emit and write times per frame; the debug overlay MUST show bytes, elisions and the four times.
Falsifier: A rendered frame reports zero bytes, or a listed statistic is absent from the overlay.

[RAS-007] The hit grid MUST be allocated on its first write and a frame that never wrote it MUST not fill it; the next buffer MUST be cleared once per frame.
Falsifier: A renderer that received no hit write allocates the grid, or a frame clears the next buffer twice.

[RAS-008] The frame diff MUST compare rows on the buffer's column arrays in one pass that finds the first differing column; it MUST not construct cell values on the compare path.
Falsifier: The compare path constructs cell values, or the unchanged-frame bound in RAS-005 is exceeded.

## Mechanism definitions
assertion-audit: {"command":"python3 -B scripts/cairn/assertion_audit.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","crates/reactive-tui-suprtui","scripts/cairn","src","tests"],"requirements":["BAR-002"],"results":null}
blitters: {"command":"python3 -B scripts/cairn/blitters.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","crates/reactive-tui-suprtui/Cargo.toml","crates/reactive-tui-suprtui/UPSTREAM.md","crates/reactive-tui-suprtui/src","crates/reactive-tui-suprtui/tests","manual/images-and-clipboard.md","scripts/cairn","src/core/capabilities.rs","src/layout/paint_tree/cells.rs","src/layout/paint_tree/suprtui.rs","src/widgets/display/image","src/widgets/display/mod.rs"],"requirements":["BLT-001","BLT-002"],"results":"per-requirement"}
catalog-manual: {"command":"python3 -B scripts/cairn/catalog_manual.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","examples/widget_catalog","manual/display-widgets.md","scripts/cairn","src/builder/widgets/chart.rs","src/layout/colors.rs","src/theme","src/widgets/display/charts","src/widgets/display/charts.rs","tests/common"],"requirements":["BAR-006"],"results":null}
charts-goldens: {"command":"python3 -B scripts/cairn/charts_goldens.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","examples/widget_catalog","manual/display-widgets.md","scripts/cairn","src/builder/widgets/chart.rs","src/layout/colors.rs","src/theme","src/widgets/display/charts","src/widgets/display/charts.rs","tests/charts_goldens.rs","tests/common","tests/snapshots/charts"],"requirements":["BAR-004","CHT-012","CHT-013","CHT-014","CHT-023","CHT-024","CHT-025","CHT-026","CHT-027","CHT-028"],"results":"per-requirement"}
dangling-paths: {"command":"python3 -B scripts/cairn/dangling_paths.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["CHANGELOG.md","CONTRIBUTING.md","Cargo.toml","README.md","benches","bindings","docs","examples","include","manual","scripts","src","tests","verification"],"requirements":["BAR-007"],"results":null}
frame-budget: {"command":"python3 -B scripts/cairn/charts_contract.py frame-budget","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","scripts/cairn","src/app.rs","src/backend","src/builder/widgets/chart.rs","src/layout/colors.rs","src/reactive","src/theme","src/widgets/display/charts","src/widgets/display/charts.rs","tests/charts_contract.rs","tests/common"],"requirements":["BAR-005","CHT-021"],"results":"per-requirement"}
painter-goldens: {"command":"python3 -B scripts/cairn/painter_goldens.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","crates/reactive-tui-suprtui/Cargo.toml","crates/reactive-tui-suprtui/src","crates/reactive-tui-suprtui/tests","scripts/cairn","src/app","src/app.rs","src/backend","src/component/bridge.rs","src/event","src/layout/paint_tree","src/screen","tests/common","tests/painter_hit.rs","tests/snapshots/charts","tests/snapshots/renderer"],"requirements":["PNT-001","PNT-002","PNT-004"],"results":"per-requirement"}
present-pipeline: {"command":"python3 -B scripts/cairn/present_pipeline.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","crates/reactive-tui-suprtui/Cargo.toml","crates/reactive-tui-suprtui/src","crates/reactive-tui-suprtui/tests","manual/rendering-and-backends.md","scripts/cairn","src/app.rs","src/backend","tests/api_component_expansion.rs","tests/api_hook_lifecycle.rs","tests/common","tests/embedded_terminal.rs","tests/present_pipeline.rs","tests/suprtui_renderer.rs"],"requirements":["PIP-001","PIP-002"],"results":"per-requirement"}
render-alloc: {"command":"python3 -B scripts/cairn/render_alloc.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","crates/reactive-tui-suprtui/Cargo.toml","crates/reactive-tui-suprtui/src","crates/reactive-tui-suprtui/tests","scripts/cairn","src/backend","tests/suprtui_renderer.rs"],"requirements":["PNT-003","RAS-003","RAS-007"],"results":"per-requirement"}
render-bytes: {"command":"python3 -B scripts/cairn/render_bytes.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","crates/reactive-tui-suprtui/Cargo.toml","crates/reactive-tui-suprtui/src","crates/reactive-tui-suprtui/tests","scripts/cairn","src/backend","tests/suprtui_renderer.rs"],"requirements":["RAS-001","RAS-002","RAS-005","RAS-006","RAS-008"],"results":"per-requirement"}
render-replay: {"command":"python3 -B scripts/cairn/render_replay.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","crates/libghostty-vt","crates/reactive-tui-suprtui/Cargo.toml","crates/reactive-tui-suprtui/src","crates/reactive-tui-suprtui/tests","scripts/cairn","src/backend","src/layout/paint_tree","src/widgets/display/charts","src/widgets/display/charts.rs","tests/common","tests/renderer_replay.rs","tests/snapshots/charts","tests/snapshots/renderer"],"requirements":["RAS-004"],"results":"per-requirement"}
widget-bar: {"command":"python3 -B scripts/cairn/charts_contract.py widget-bar","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","scripts/cairn","src/builder/widgets/chart.rs","src/layout/colors.rs","src/theme","src/widgets/display/charts","src/widgets/display/charts.rs","tests/charts_contract.rs","tests/common"],"requirements":["BAR-003"],"results":"per-requirement"}
workspace-gates: {"command":"python3 -B scripts/cairn/workspace_gates.py","cwd":null,"documents":[],"identity":{"env":[],"image":null,"tools":{}},"inputs":["Cargo.lock","Cargo.toml","benches","build.rs","crates","examples","scripts/cairn","src","tests"],"requirements":["BAR-001"],"results":null}

## Builder claims
mechanism workspace-gates Q1 observed: Receipt a7e5e605096e (bound in review metadata): 'BAR-001 violated: gates failing: cargo clippy --locked, cargo test --locked'. In this commitment receipt dfcb85a25a8e also failed: 'BAR-001 violated: gates failing: cargo test --locked' with 3 failed lib tests, api_event_routing removed_callback_releases_its_capture_and_cannot_run_again and api_paint_properties transformed_callbacks_follow_visible_and_clipped_bounds.
mechanism workspace-gates Q2 observed: Both failures are commands BAR-001 names exiting non-zero on real defects: the second was a regression from this commitment (App kept the previous frame's tree, so a removed button's closure stayed alive; the App test harness did not forward the hit grid, so a rotated clip was not exact), fixed in 22c7901e, after which receipt 0a350a7b0a17 passed.
mechanism assertion-audit Q1 observed: Receipt f686f05df411: 'BAR-002 violated: tests without an assertion and not named smoke_:' and a list of test functions.
mechanism assertion-audit Q2 observed: Each listed test had no assertion; the script ran to completion over the tree, so the failure was the falsifier, not a parse error.
mechanism widget-bar Q1 observed: Receipt 4435e9d56042: 'BAR-003 reason: 16 hard-coded colors in chart code; 1 passed'.
mechanism widget-bar Q2 observed: The count is of color literals in delivered widget code, the falsifier; the rest of the test passed in the same run.
mechanism charts-goldens Q1 observed: Receipt 8da6bdb2107e: 'BAR-004 reason: no checked-in wide golden'.
mechanism charts-goldens Q2 observed: There was no wide golden to compare, the falsifier's missing-golden clause; the same run compiled and ran the chart contract tests.
mechanism frame-budget Q1 observed: Receipt c4c9cf63a485: 'BAR-005 reason: ... fewer than 12 frames painted inside the harness's 3 s deadline at 700x200: per-frame work exceeds 16.6 ms'. In this commitment receipt fdc6bade0067 failed with 'per-frame work 17.03 ms exceeds 16.6 ms at 700x200' at load average about 20; five local release runs then passed, and the rerun of the check at lower load passed on the same tree. Receipt 5471169a28ca then failed on the real violation, 'per-frame work 18.08 ms exceeds 16.6 ms at 700x200': a per-frame split showed every frame at 2-4 ms except the first frame at the chart's size (14-31 ms), 10.2 ms of it the chart blocking on its worker; 423f6771 caps that wait at 4 ms (escalation 3f0fc0d6, developer ok).
mechanism frame-budget Q2 observed: Both are the falsifier measured: per-frame work above 16.6 ms. The second came from host load, not code: the tree difference was docstrings only, and the rerun at lower load passed.
mechanism catalog-manual Q1 observed: Receipt 18c1a12d3902: 'BAR-006 violated:' with missing catalog pages and manual headings.
mechanism catalog-manual Q2 observed: Each line names a missing page or heading for a delivered chart type, the falsifier.
mechanism dangling-paths Q1 observed: Receipt 60330a847b77 on a fixture that added 'scripts/no-such-file.py' to CONTRIBUTING.md: 'BAR-007 violated: references to paths git does not track: CONTRIBUTING.md: scripts/no-such-file.py'. In this commitment receipt b84dec0369dc failed on real references: this commitment's mechanism docstrings named crate tests relative to the crate, and Cairn's tracked brief under .cairn/output quoted a violating example.
mechanism dangling-paths Q2 observed: Both list paths git does not track, the falsifier; the docstrings were fixed and the checker now skips Cairn's ledger records (b210b275), and a fixture file with a missing path still fails (checked with --fixture).
mechanism render-bytes Q1 observed: Bound fail receipts: RAS-001 59f59685a002, RAS-002 07e37fdf2aac and RAS-008 12d2a304be01 printed 'render_bytes did not run: error: no test target named `render_bytes`'; RAS-005 7fc03f2bbe56 printed 'unchanged 512x512 frame took 891.771µs'; RAS-006 53e6d1aa74c8 printed 'RenderStats lacks attr_elided, attr_emitted, bg_elided, ... write_ns'.
mechanism render-bytes Q2 observed: RAS-005 and RAS-006 failed on the stated violation (the bound, the missing statistics). For RAS-001, RAS-002 and RAS-008 the bound receipt shows only that the mechanism fails when its test is absent, not that the test catches the violation; the mutation runs under Q3 for those requirements are the evidence that it does.
mechanism render-alloc Q1 observed: Bound fail receipts: PNT-003 83865ddd0335 printed 'frame element cloned per present: element.clone(), self.frame.clone()'; RAS-003 0166847045bb and RAS-007 02a63f23540a printed 'render_alloc did not run: error: no test target named `render_alloc`'.
mechanism render-alloc Q2 observed: PNT-003 failed on the stated violation (a clone per present). For RAS-003 and RAS-007 the bound receipt shows only a missing test; the mutation runs under Q3 show the tests catch the violation.
mechanism render-replay Q1 observed: Receipt 6c648570aea5: 'RAS-004 reason: no recorded screens under tests/snapshots/renderer'.
mechanism render-replay Q2 observed: A frame with no recording is the falsifier's second clause; the mutation run under RAS-004 Q3 shows a changed byte stream fails its recording.
mechanism painter-goldens Q1 observed: Bound fail receipts: PNT-002 3625333911aa printed 'hit_bounds still scans cells through inside_masks'; PNT-001 735ed2a09fe5 and PNT-004 dae11c8a2fdc printed 'painter_hit did not run: error: no test target named `painter_hit`'. Receipt 16802cc729c8 on d036d974 failed all three with 'painter_hit did not run: error: test failed' and no summary line; the same command then passed from a shell, and 07571618 makes the reason carry cargo's Caused by line and any signal.
mechanism painter-goldens Q2 observed: PNT-002 failed on the stated violation. For PNT-001 and PNT-004 the bound receipt shows only a missing test; the mutation runs under Q3 show the tests catch the violation. Receipt 16802cc7 was not the violation: the binary produced no test summary, which the old reason could not explain; see Q5.
mechanism present-pipeline Q1 observed: Bound fail receipts: PIP-002 ef233998be0c printed 'manual still says the App keeps geometry until a frame is presented successfully'; PIP-001 9b5f05026a42 printed 'present_pipeline did not run: error: no test target named `present_pipeline`'.
mechanism present-pipeline Q2 observed: PIP-002 failed on its manual clause. For PIP-001 the bound receipt shows only a missing test; the mutation run under PIP-001 Q3 shows the timing test catches a present that waits for its flush.
mechanism blitters Q1 observed: Bound fail receipts de136c9d2e68 (BLT-001) and 8fea37328234 (BLT-002), after the mechanism was extended to the application tests in 416305c0, printed 'crate: blitters did not run: error: no test target named `blitters` in `reactive-tui-suprtui` package'.
mechanism blitters Q2 observed: These show only that the mechanism fails with no blitters; the evidence that the tests catch the violation is two mutation runs on 2026-09-23 in crates/reactive-tui-suprtui/src/blit.rs: a solver that only ever considered one split failed 'Braille ...: chose 0b10 with error 222942.857..., a split has 0', and an opaque background under a transparent pixel failed 'Braille hole 0: opaque background'; the file was restored byte for byte.
requirement BAR-001 Q3 observed: Receipt a3bdca43424c ran build, fmt --check, clippy -D warnings, doc --no-deps and test --workspace --no-fail-fast and all five exited zero.
requirement BAR-001 Q4 observed: 0d928653 documented 615 public items in the renderer crate and 21 in the crossterm fork and made every crate warn on missing docs and deny broken intra-doc links, so the clippy gate now fails on an undocumented item; nothing checks that the comments are true beyond the sampled reading and the agents' reports.
requirement BAR-002 Q3 observed: Receipt 826704cf0ffd: the assertion audit reports no test without an assertion; every test added this commitment asserts on output, bytes, hits or timing.
requirement BAR-002 Q4 observed: Several new tests assert inequalities (timing bounds, 'at most') rather than exact values; the audit counts them as asserting.
requirement BAR-003 Q3 observed: Receipt 3faaa70e3d36 passes: the widget bar finds no hard-coded color in delivered widget code.
requirement BAR-003 Q4 observed: The blitter code carries colors from image pixels, not literals; the audit does not look at crates/. BAR-003's stale-geometry clause: bar_003_a_resized_chart_never_paints_the_picture_drawn_for_the_old_size (tests/charts_contract.rs) failed before b9620946 with frames [busy=false ink=38x12, busy=true, busy=false ink=665x200] and passes after it; the_first_frame_after_a_resize_shows_components_their_new_size and the anchored-dialog check fail with the layout pass or the anchor refresh removed (escalation f8eb1c52, developer ok).
requirement BAR-004 Q3 observed: Receipt d7af5e2dc0d5 passes with the chart goldens, which the painter and rasterizer changes did not alter.
requirement BAR-004 Q4 observed: The goldens are the debug backend's grid; the rasterizer's byte stream for them is covered by RAS-004's replay, not by BAR-004.
requirement BAR-005 Q3 observed: Receipt 6e4c2ae6f68a: per-frame work at 700x200 stays under 16.6 ms in release.
requirement BAR-005 Q4 observed: The measure is a maximum over 12 frames on a shared host. Before 423f6771 the first frame at the new size blocked for the whole raster (10.4 ms at 700x200) and read 14-31 ms; with the wait capped at 4 ms ten release runs passed and the slowest frame read 6-8 ms. A chart larger than about 400 by 100 now paints one busy, empty frame when it appears or resizes.
requirement BAR-006 Q3 observed: Receipt 0067835aebc5 passes; the catalog and manual still describe every chart type.
requirement BAR-006 Q4 observed: manual/images-and-clipboard.md gained the block fallback text; BAR-006 does not read it, BLT-002's mechanism does.
requirement BAR-007 Q3 observed: Receipt e637d441a968: no tracked file references a missing path.
requirement BAR-007 Q4 observed: The checker skips the ledger under .sudus/ and its former name .cairn/ (48998811, after sudus migrate); a real dangling link inside a ledger record would not be caught, by design. It checks only paths under the listed top-level directories, so links into dot-directories are not checked; the manual's stale .cairn links were found by hand and fixed in 48998811.
requirement RAS-001 Q3 observed: emit_cell tracks the cursor and emits nothing at the tracked position, CHA on the same row and CUP otherwise; ras_001_ tests two adjacent cells and a same-row jump. A mutation run on 2026-09-23 changed crates/reactive-tui-suprtui/src/render.rs to `Some((cx, cy)) if false && cx == x && cy == y => stats.moves_elided += 1,` and ran `cargo test --locked -p reactive-tui-suprtui --test render_bytes -- ras_001_`: exit 101, 'test result: FAILED. 0 passed; 2 failed; 0 ignored; 0 measured; 3 filtered out; finished in 0.00s', panic 'assertion `left == right` failed: two adjacent cells: [27, 91, 63, 50, 48, 50, 54, 104, 27, 91, 63, 50, 53, 108, 27, 91, 48, 109, 27, 91, 50, 59, 51, 72, 27, 91'; the file was restored byte for byte. Receipt 7d917f1f3e3e.
requirement RAS-001 Q4 observed: After a multi-codepoint cluster the tracked column becomes unknown and the next cell uses CUP; how a real host renders such a cluster is covered only by the vt100 and ghostty models in RAS-004.
requirement RAS-002 Q3 observed: push_color and push_attributes emit only on change, off codes per attribute, and ESC [0m only in start_frame (guarded by state.started) and before the sync-reset. A mutation run on 2026-09-23 changed crates/reactive-tui-suprtui/src/render.rs to `if true || fg != state.fg {` and ran `cargo test --locked -p reactive-tui-suprtui --test render_bytes -- ras_002_`: exit 101, 'test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 4 filtered out; finished in 0.00s', panic 'assertion `left == right` failed: [27, 91, 63, 50, 48, 50, 54, 104, 27, 91, 63, 50, 53, 108, 27, 91, 48, 109, 27, 91, 49, 59, 49, 72, 27, 91, 51, 56, 59, 50, 59'; the file was restored byte for byte. Receipt 7d917f1f3e3e.
requirement RAS-002 Q4 observed: Attribute transitions outside the test's cases (underline color reset 59, overline off 55) are exercised by the replay recordings, not asserted byte for byte.
requirement RAS-003 Q3 observed: Cells append to one reused Vec<u8> with push_u32 and push_hex2; ras_003_ counts 0 allocations for a 10,000-cell render. A mutation run on 2026-09-23 changed crates/reactive-tui-suprtui/src/render.rs to `std::hint::black_box(String::from("per-cell"));` and ran `cargo test --locked -p reactive-tui-suprtui --test render_alloc -- ras_003_`: exit 101, 'test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 1 filtered out; finished in 0.02s', panic 'assertion `left == right` failed: render of a 10,000-cell frame allocated 10000 times'; the file was restored byte for byte. Receipt f16c8d1f24b3.
requirement RAS-003 Q4 observed: Allocation outside Renderer::render (painter, App, worker) is not counted; PNT-003 covers only the frame copy.
requirement RAS-004 Q3 observed: tests/renderer_replay.rs replays 7 renderer frames and 15 chart goldens through vt100 and ghostty and compares 22 recordings. A mutation run on 2026-09-23 changed crates/reactive-tui-suprtui/src/render.rs to `push_cha(out, x + 2);` and ran `cargo test --locked -p reactive-tui --features embedded-terminal --test renderer_replay`: exit 101, 'test result: FAILED. 0 passed; 2 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.04s', panic 'assertion `left == right` failed: chart_line_mini: the vt100 screen differs from its recording'; the file was restored byte for byte. Receipt 56196ec6b41b.
requirement RAS-004 Q4 observed: The recordings were made from the old rasterizer, so they prove equivalence, not correctness; no real terminal emulator (kitty, ghostty GUI) replays them.
requirement RAS-005 Q3 observed: The release bench, best of three, reads under 12 bytes per cell for a 200x50 repaint, under 1 ms unchanged and under 15 ms full for 262,144 cells; the 1 ms bound is the developer's ruling of 2026-09-22 (escalation a648dc31). Receipt 7d917f1f3e3e.
requirement RAS-005 Q4 observed: The bound is host-dependent; the mechanism runs the bench up to three times, so one loaded run cannot fail it, but a slower host could.
requirement RAS-006 Q3 observed: RenderStats carries every listed field and the overlay test finds bytes, elisions and the four times on screen. Receipt 7d917f1f3e3e.
requirement RAS-006 Q4 observed: The overlay's placement on a terminal narrower than its text is not tested.
requirement RAS-007 Q3 observed: The hit grid is empty until the first write (ensure_hit_grid) and the renderer never clears the next buffer; the painter clears it once. A mutation run on 2026-09-23 changed crates/reactive-tui-suprtui/src/render.rs to `pub fn render(&mut self, force: bool) -> RenderStatus {` and ran `cargo test --locked -p reactive-tui-suprtui --test render_alloc -- ras_007_`: exit 101, 'test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 1 filtered out; finished in 0.00s', panic 'no hit write, yet a grid exists'; the file was restored byte for byte. Receipt f16c8d1f24b3.
requirement RAS-007 Q4 observed: A painter that skipped its clear would leave stale cells; tests/suprtui_renderer.rs ras_007_ covers the App path, not every backend.
requirement RAS-008 Q3 observed: row_first_change compares the five column arrays by slice and finds the first differing column with cell_eq_at, which reads the arrays without building a Cell. A mutation run on 2026-09-23 changed crates/reactive-tui-suprtui/src/buffer/mod.rs to `&& self.attributes[start..end] == other.attributes[start..end];` and ran `cargo test --locked -p reactive-tui-suprtui --test render_bytes -- ras_008_`: exit 101, 'test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 4 filtered out; finished in 0.00s', panic 'assertion `left == right` failed'; the file was restored byte for byte. Receipt 7d917f1f3e3e.
requirement RAS-008 Q4 observed: That no Cell is built on the compare path is a property of the code read in review, and the RAS-005 unchanged bound; no test inspects the source.
requirement PNT-001 Q3 observed: Nodes with a translation-only transform and no mask fill backgrounds and place text by subtraction (`plain` in paint_node); pnt_001_ compares five fast-path screens with the recordings of the general path. A mutation run on 2026-09-23 changed src/layout/paint_tree/suprtui.rs to `(x as f32 + half + offset_x + 1.0, y as f32 + offset_y)` and ran `cargo test --locked -p reactive-tui --test painter_hit -- pnt_001_`: exit 101, 'test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 3 filtered out; finished in 0.00s', panic 'assertion `left == right` failed: styled_layout: the fast path's screen differs from the recorded general path'; the file was restored byte for byte. Receipt 75193def8bae.
requirement PNT-001 Q4 observed: Cell grids, images and the cursor layer keep their own placement code; the recordings cover identity nodes only.
requirement PNT-002 Q3 observed: mark_hits writes each node's element index per painted cell in paint order, exact under masks and z-order, and the router resolves hits by the grid first; pnt_002_ checks masked and layered grids, hit_at, and click dispatch. Receipt 75193def8bae. 22c7901e made the App test harness forward the grid, after api_paint_properties showed that without it a rotated clip routed a click to a clipped node.
requirement PNT-002 Q4 observed: Backends without a hit grid (crossterm, direct tty) still route by bounds, which are not exact under rotated clips.
requirement PNT-003 Q3 observed: The backend holds the frame as Arc<Element> and sends a handle per present; the test counts handles and finds no growth over repeated presents. Receipt f16c8d1f24b3.
requirement PNT-003 Q4 observed: render_frame still makes the one copy the requirement allows (Arc::new(element.clone())).
requirement PNT-004 Q3 observed: LayoutCache reuses the Taffy layout when same_layout_inputs holds; pnt_004_ checks reuse, relayout on changed text and on resize. A mutation run on 2026-09-23 changed src/layout/paint_tree/suprtui.rs to `fn same_node(a: &NodeSpec<'_>, b: &NodeSpec<'_>) -> bool {` and ran `cargo test --locked -p reactive-tui --test painter_hit -- pnt_004_`: exit 101, 'test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 3 filtered out; finished in 0.00s', panic 'an unchanged spec must reuse the layout'; the file was restored byte for byte. Receipt 75193def8bae.
requirement PNT-004 Q4 observed: Images and cell grids compare by Arc pointer, so an equal grid in a new Arc forces a relayout: correct, but slower than needed.
requirement PIP-001 Q3 observed: The worker replies with geometry and then flushes; a rendezvous channel keeps one frame in flight; pip_001_ times present against an 80 ms flush. A mutation run on 2026-09-23 changed src/backend/suprtui.rs to `let _ = renderer.backend_mut().flush_pending();` and ran `cargo test --locked -p reactive-tui --test present_pipeline -- pip_001_`: exit 101, 'test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 1 filtered out; finished in 0.08s', panic 'present waited for its own flush: 80.55551ms with a 80ms flush'; the file was restored byte for byte. Receipt 56a53a736942.
requirement PIP-001 Q4 observed: Callers that read the output right after present must now wait: 22c7901e added Backend::sync (a no-op by default) and ScreenManager::sync and made 13 tests wait; five widget tests stay sensitive to frame pacing under an artificial 30 ms write delay, which real load has not triggered.
requirement PIP-002 Q3 observed: A flush failure is reported by the next present, sync or shutdown; geometry falls back to the last acknowledged frame and the next frame repaints fully; the manual sentence was updated. Receipt 56a53a736942.
requirement PIP-002 Q4 observed: App's fallback republishes the acknowledged frame's animation targets; since 22c7901e it keeps those values instead of the frame's tree. The accessibility snapshot on the failure path is not covered.
requirement BLT-001 Q3 observed: crates/reactive-tui-suprtui/src/blit.rs offers half-block, quadrant, sextant, octant and braille; blit_block tries every split that leaves pixel 0 clear and compares scores exactly in integers; a transparent pixel forces a transparent background. The crate test checks 305 fixed blocks per tier against every split, glyph ranges and the Unicode 16 names; the application tests check the widget's fallback, grid backgrounds through the painter and the pinned Auto order. Receipt 75c7a8524b7e.
requirement BLT-001 Q4 observed: Whether a real terminal's font draws the octant and sextant glyphs is not tested; the per-terminal table is judgment, and Auto keeps chafa and viu ahead of the blitters by the developer's ruling (escalation 81705d17).
requirement BLT-002 Q3 observed: blit::choose applies the environment override, then the application's, then ASCII without unicode, then the table, then sextant; tests cover every tier, case-insensitive identities, both overrides and parse errors; the application test covers set_image_blitter and clearing it. Receipt 75c7a8524b7e.
requirement BLT-002 Q4 observed: The host identity is TERM_PROGRAM or TERM only; inside tmux or over SSH the table sees the multiplexer or the remote TERM. An invalid REACTIVE_TUI_BLITTER value is logged once and ignored.
commitment suprtui Q5 observed: Done-when checks. No per-cell allocation on the render path: ras_003_ counts zero allocations for a 10,000-cell render, and a String per cell was caught with 10,000 allocations. No per-cell reset: the renderer writes ESC [0m only in start_frame, guarded by state.started, and once before the sync-reset (crates/reactive-tui-suprtui/src/render.rs lines 845 and 1098; line 641 is the terminal restore sequence); ras_002_ asserts at most one reset at each end. The PIP-002 manual sentence was replaced. Risks: three timing bounds (RAS-005, BAR-005, PIP-001) depend on this host's load. BAR-001 failed six times on 2026-09-23 without a code fault: four rustc internal compiler errors 'uninterned StableCrateId: StableCrateId(0)', each in a different test crate and none reproducible alone, which upstream tracks as an incremental-compilation bug (rust-lang/rust#149697, rust-clippy#14572), one mold undefined-symbol link error, and rustc segfaults the kernel logged; c9cde8c7 builds the checks with CARGO_INCREMENTAL=0. The host also crashed unrelated programs (chrome, nginx, node) in the kernel log; the developer turned XMP off, and rustc still segfaulted once afterwards, so the CPU remains suspect. c9cde8c7 also stopped the Ghostty sys crate rebuilding on every cargo command (its build script watched a missing path). This commitment supersedes suprtui-renderer (1065554a) because RAS-005's bound was revised under it; the records moved from .cairn to .sudus with sudus migrate (9851f8cf). Pipelined presentation changed when output lands: 13 tests read output before the write and were fixed with sync. The documentation pass fixed two defects in the renderer crate and five wrong doc statements.
commitment suprtui Q6 observed: Not tested: any real terminal emulator drawing the new bytes or the blitter glyphs (kitty, ghostty, foot, WezTerm, GNOME Terminal); scripts/check-api-images.py's host captures were not rerun; macOS and Windows; the crossterm and direct tty backends' hit routing; the SuprTUI modules the application never calls (backlog item b12e1176 suprtui-unused-modules); the Kitty image path's quadrant fallback on a host without Kitty beyond the table fix; animated GIF frames through the blitters on the worker at large sizes. Also not tested: the resize layout pass on backends that return no layout (they keep the old order and one stale frame); the chart's busy state with a real screen reader; the 1.x-era check scripts that still write into the reserved .cairn/ directory, captured for the backlog.

## Builder findings

## Interface obligations

## Changed paths (start snapshot to reviewed snapshot; A added, M modified, D deleted)
M docs/decisions.jsonl

## Exclusion manifest (classes and paths, never contents)

## Boundary
Harness claude_code, model any, transport any.
You may read only the projection directory. Boundary: unenforced.
This projection omits network_exclude and credential paths. It is an egress boundary, not an information-flow proof: it cannot detect a secret a person or primary coding agent copied into ordinary prose, and it does not govern Git pushes.

## Your work
For each mechanism (Q1, Q2): try to make it pass without the behavior, make it fail for a setup reason, and find an input it reads but does not declare. For each Q3: try to reach the falsifier with an input. For each Q4: compare the changed paths above with the claim and find touched paths it omitted. For Q5 and Q6: look where the builder said not to. Every interface obligation gets a caller-level attempt. Write one attempt per question and target, one per interface path, and number your findings from 1.
