# ripwire quality acks v1 — written by --quality-ack; a finding stays suppressed until it worsens past its acked magnitude
# format: ack <kind> <16-hex-key> <ackNow> <reason to end of line> — one per line, kept SORTED by (kind,key) on every write (merge-friendly)
ack dead-code:preexisting e2dd0eba047f74d4 0 UnixTty restore is required by the PlatformTty trait; cargo rejected its removal with E0046, so this is dynamic-dispatch reachability missing from Ripwire
ack duplication 299318b2dde1de5d 112 TRL-002 uses two explicit PTY lifecycle fixtures; a shared abstraction would be premature under the Rule of Three, and the SignalComponent match is unrelated code
ack duplication 6716b3b98a9864cc 25 TRL-002 uses two explicit PTY lifecycle fixtures; a shared abstraction would be premature under the Rule of Three, and the SignalComponent match is unrelated code
ack duplication 887a05e230369a41 156 TRL-002 uses two explicit PTY lifecycle fixtures; a shared abstraction would be premature under the Rule of Three, and the SignalComponent match is unrelated code
ack new-clone-of-reused-helper 6716b3b98a9864cc 5 SignalComponent render is a PTY readiness probe and only token-similar to the unrelated os_error helper
ack short-horizon-churn 3e6214eea6a0306e 8 TRL-001 must wrap the existing application and renderer entry points so owned terminal state drops before panic text is reprinted
ack short-horizon-churn 4e797352285e10ab 2 TRL-004 must replace the unbounded stty child with the shared deadline, process-group termination, and reap path
ack short-horizon-churn eeda96bd625b29d3 2 TRL-003 must sanitize child-provided OSC title text at the parser boundary before both screen state and TitleChanged events consume it
ack short-horizon-churn f0aca3a6174bda76 6 TRL-002 must remove process-global panic restoration from UnixTty initialization so one thread cannot restore another terminal owner
ack short-horizon-churn fee63597aeee73f8 18 TRL-001 must wrap the existing application and renderer entry points so owned terminal state drops before panic text is reprinted
